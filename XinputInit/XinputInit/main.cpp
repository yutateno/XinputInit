#include <Windows.h>
#include <Xinput.h>
#include <stdio.h>

int main()
{
	/*XINPUT_STATE input;
	input.dwPacketNumber = XInputGetState(0, &input);

	printf("%d", input.Gamepad.bLeftTrigger);*/

	return 0;
}